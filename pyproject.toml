[tool.uv.workspace]
members = ["builders/server", "builders/sdk"]

[tool.pytest.ini_options]
pythonpath = ["builders/server", "builders/sdk"]
testpaths = ["builders/server/tests", "builders/sdk/tests"]
python_files = ["test_*.py", "bench_*.py"]
markers = [
    "integration: integration tests requiring a real postgres (via testcontainers)",
    "benchmark: performance benchmarks (slow, not run by default)",
]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["B", "E", "F", "I", "PIE", "SIM", "T20", "UP"]

[tool.ruff.lint.per-file-ignores]
"builders/server/benchmarks/**" = ["T20"]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
warn_unused_configs = true
mypy_path = "builders/server"

[project]
name = "datastream-sdk"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
]

[project.optional-dependencies]
pandas = ["pandas>=2.0"]
polars = ["polars>=1.0"]

[dependency-groups]
dev = [
    "pandas>=2.0",
    "polars>=1.0",
    "pytest>=9.0.2",
]
